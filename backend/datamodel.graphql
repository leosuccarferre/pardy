type User {
  id: ID! @unique
  name: String!
  email: String!
}

type Clue {
  id: ID! @unique
  category: String!
  difficulty: Int!
  question: String!
  choices: String!
  answer: String!
  airDate: DateTime!
  # createdAt: DateTime!
  # updatedAt: DateTime!
  # user: User!
}
