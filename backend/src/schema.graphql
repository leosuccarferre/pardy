# import * from './generated/prisma.graphql'

type Mutation {
    createClue(
        category: String,
        difficulty: Int,
        question: String,
        choices: String,
        answer: String,
        airDate: DateTime
    ):Item!
}

type Query {
    clues: [Clue]!
}